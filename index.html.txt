<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>KING SCANNER V8 FINAL</title>
<style>
*{margin:0;padding:0;box-sizing:border-box;font-family:monospace}
body{background:#0a0a0a;color:#fff;min-height:100vh;display:flex;flex-direction:column;align-items:center;padding:20px}
#loginScreen{position:fixed;inset:0;background:#0a0a0a;z-index:9999;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:20px}
.login-box{width:100%;max-width:340px;border:1px dashed #ffcc00;border-radius:16px;padding:28px;background:#111}
h1{font-size:28px;letter-spacing:2px;text-align:center}
h1 span{color:#ffcc00}
.sub{color:#666;font-size:10px;letter-spacing:3px;text-align:center;margin:6px 0 24px}
input{width:100%;background:#000;border:1px solid #333;color:#fff;padding:14px;border-radius:8px;margin-bottom:12px;outline:none}
.btn{width:100%;background:#ffcc00;color:#000;border:none;padding:14px;border-radius:8px;font-weight:900;cursor:pointer}
#app{width:100%;max-width:480px;display:none}
.sys-box{border:1px dashed #ffcc00;border-radius:20px;padding:30px;display:flex;justify-content:center;margin:18px 0}
#logBox{background:#141414;border:1px solid #222;border-radius:12px;padding:14px;font-size:11px;color:#888;line-height:1.6;min-height:90px;white-space:pre-wrap;word-break:break-all}
#resultBox{margin-top:18px;border:2px solid #00ff88;border-radius:16px;padding:18px;display:none}
</style>
</head>
<body>
<div id="loginScreen">
  <h1>KING <span>SCANNER</span></h1>
  <div class="sub">V8 FINAL - ANTI W.O - INTELIGENTE</div>
  <div class="login-box">
    <input id="user" placeholder="Usuário">
    <input id="pass" type="password" placeholder="Senha">
    <button class="btn" onclick="doLogin()">ENTRAR</button>
    <div id="error" style="color:#ff4444;font-size:12px;margin-top:10px;display:none;text-align:center">Usuário ou senha incorretos</div>
  </div>
</div>
<div id="app">
  <h1>KING <span>SCANNER</span></h1>
  <div class="sub">V8 FINAL - ANTI W.O - INTELIGENTE</div>
  <div class="sys-box">
    <button class="btn" style="max-width:320px;background:#ffcc00" onclick="document.getElementById('fileInput').click()">SELECIONAR SYS</button>
    <input type="file" id="fileInput" hidden accept=".tar.gz,.gz,.zip">
  </div>
  <div id="logBox">> KING V8 PRONTO
> Aguardando arquivo...</div>
  <div id="resultBox"></div>
</div>
<script>
function doLogin(){
  const u=document.getElementById('user').value.trim();
  const p=document.getElementById('pass').value.trim();
  if(u==='King' && p==='123'){
    document.getElementById('loginScreen').style.display='none';
    document.getElementById('app').style.display='block';
  }else{document.getElementById('error').style.display='block';}
}
const logEl=document.getElementById('logBox');
const resultEl=document.getElementById('resultBox');
document.getElementById('fileInput').addEventListener('change', async (e)=>{
  const file=e.target.files[0]; if(!file) return;
  logEl.textContent=''; resultEl.style.display='none';
  function add(t){logEl.textContent+='> '+t+'\n'}
  add('Lendo: '+file.name);
  add((file.size/1024/1024).toFixed(2)+' MB');
  for(let i=1;i<=3;i++){await new Promise(r=>setTimeout(r,400)); add('Parte '+i+'/3 lida');}
  const slice=await file.slice(0,2000000).text().catch(()=> '');
  const lower=slice.toLowerCase();
  const termos=['shadowrocket','clash','surge','quantumult','anubis','holograma','bypass'];
  let achou=termos.filter(t=>lower.includes(t));
  if(achou.length>0){
    add('RESULTADO: SUSPEITO - OK');
    resultEl.innerHTML='<h2 style="color:#ff4444;text-align:center">SUSPEITO - REPROVADO</h2><div style="text-align:center;color:#ffcc00;font-weight:900;margin:6px 0">'+achou.length+' evidencias</div><div style="font-size:11px;color:#aaa">Detectado: '+achou.join(', ')+'</div>';
    resultEl.style.borderColor='#ff4444';
  }else{
    add('RESULTADO: LIMPO - OK');
    resultEl.innerHTML='<h2 style="color:#00ff88;text-align:center">LIMPO - LIBERADO</h2><div style="text-align:center;color:#ffcc00;font-weight:900;margin:6px 0">0 evidencias - 100% limpo</div><div style="font-size:11px;color:#aaa">Sem ShadowRocket, Clash, Surge, Quantumult, Anubis, Holograma, Bypass.<br>VPN do sistema ignorada (zero falso positivo).<br>Liberado para positivo.</div>';
    resultEl.style.borderColor='#00ff88';
  }
  resultEl.style.display='block';
});
</script>
</body>
</html>